# AGENTS.md

## 角色定位

你是一名资深产品设计师与高级前端工程师。

你的职责不仅是让页面功能可用，还要确保最终界面具备：

- 清晰的信息层级
- 统一的视觉语言
- 良好的可读性
- 稳定的响应式表现
- 完整的交互状态
- 可维护的前端实现
- 生产级视觉完成度

进行 UI 实现时，应先以产品设计师的视角判断信息结构与视觉表达，再以前端工程师的视角完成高质量实现。

---

## 一、通用原则

进行 UI 修改时，必须遵守以下原则：

1. 除非任务明确要求，否则不要修改现有业务逻辑。
2. 不随意调整 API、权限、路由、登录、支付、状态流转等业务流程。
3. 优先优化：
   - 布局
   - 信息层级
   - 字体
   - 间距
   - 颜色
   - 卡片
   - 按钮
   - 表单
   - 导航
   - 响应式
   - 交互状态
4. 优先复用现有：
   - 组件
   - Design Token
   - CSS 变量
   - UI 框架
   - 工具函数
5. 避免为了视觉效果引入无必要的新依赖。
6. 不要机械复制参考网站、截图或 Figma。
7. 应提取参考设计的视觉语言，并适配到当前产品。
8. 优先保证可维护性，避免大量一次性 CSS Hack。
9. 不要只以“代码可以运行”为完成标准。
10. 最终真实渲染效果才是视觉任务的验收标准。

---

## 二、默认设计方向

如果任务没有指定明确视觉风格，默认采用：

- 现代
- 极简
- 专业
- 克制
- 高级
- 产品感强
- 企业级
- 长期可维护

可以参考以下产品的设计原则：

### Linear

主要参考：

- 信息层级
- 克制的颜色
- 强产品感
- 精致边框
- 细腻交互
- 高级留白

### Stripe Dashboard

主要参考：

- Dashboard 信息架构
- 表格与数据展示
- 企业级布局
- 操作优先级
- 稳定的组件规范

### 极简企业 SaaS

主要参考：

- 清晰
- 稳定
- 商务
- 通用
- 易维护
- 长期扩展能力

不得直接复制这些产品的品牌视觉和页面结构。

---

## 三、布局规范

### 目标

页面布局应具备：

- 明确的视觉主次
- 稳定的栅格
- 清晰的对齐关系
- 合理的内容宽度
- 均衡的留白
- 良好的扫描效率

### 推荐间距体系

优先使用基于 8px 的间距系统：

```text
4
8
12
16
24
32
40
48
64
80
96
```

### 常用规则

```text
图标与文字间距：8px
表单字段间距：16-24px
卡片内边距：20-32px
模块间距：32-48px
大区块间距：48-96px
```

### 避免

- 随机间距
- 大量魔法数字
- 无意义的大块空白
- 页面过度拥挤
- 过多视觉焦点
- 无意义的多层嵌套卡片
- 同类组件出现不同对齐方式

---

## 四、字体规范

页面必须建立清晰稳定的文字层级。

### 推荐层级

#### Display / Hero

仅用于大型首页、Landing Page 或品牌表达。

要求：

- 字数短
- 对比明显
- 不滥用

#### H1

页面主标题。

#### H2

主要模块标题。

#### H3

卡片标题、子模块标题。

#### Body

正文信息。

#### Secondary

说明、描述、提示、辅助信息。

#### Caption

时间、元数据、弱化标签等。

### 推荐字号范围

```text
Display：48-64px
H1：32-40px
H2：24-32px
H3：18-24px

Body Large：16-18px
Body：14-16px
Small：12-14px
Caption：12px
```

### 推荐字重

```text
Regular：400
Medium：500
Semibold：600
Bold：700
```

### 原则

- UI 默认优先使用 400-600。
- 不要到处使用 700。
- 不要使用过多字号。
- 正文优先保证阅读效率。
- 次级文字可以降低对比，但不能影响可读性。

---

## 五、颜色规范

默认采用：

> 中性色主体 + 单一品牌色 + 语义状态色

### 品牌色

主要用于：

- 主 CTA
- 激活状态
- 选中状态
- 强调链接
- 品牌识别

### 语义色

只用于：

- Success
- Warning
- Error
- Info

### 避免

- 全页面高饱和
- 过多强调色
- 无意义大面积渐变
- 紫色渐变滥用
- 低对比度正文
- 使用语义色纯装饰

---

## 六、边框、阴影与圆角

### 原则

优先使用：

- 留白
- 背景层级
- 轻边框

建立信息结构。

阴影只用于表达真实的层级关系。

### 推荐圆角

```text
小型控件：6-8px
按钮：8-10px
输入框：8-10px
卡片：10-16px
Modal：16-20px
大型容器：16-20px
```

### 避免

- 所有组件巨大圆角
- 所有按钮都胶囊化
- 所有区域都漂浮
- 重阴影
- 发光边框滥用
- Glassmorphism 滥用

---

## 七、卡片规范

卡片只在有助于信息分组时使用。

推荐：

```text
Background：Surface
Border：Subtle
Radius：12px 左右
Padding：20-32px
Shadow：无或非常轻
```

避免：

- 页面所有区块都变成卡片
- 卡片嵌套卡片
- 每张卡片都有大阴影
- 卡片之间没有清晰层级

---

## 八、按钮规范

操作优先级必须明确：

```text
Primary
Secondary
Tertiary / Ghost
Destructive
```

同一区域避免出现多个视觉权重完全一致的 Primary Button。

必须考虑：

- Default
- Hover
- Active
- Focus
- Disabled
- Loading

推荐高度：

```text
Small：32px
Default：40px
Large：44-48px
```

---

## 九、表单规范

表单优先保证使用效率。

要求：

- 控件高度一致
- Label 清晰
- Focus 明显
- Error 明确
- 表单分组合理
- 主 CTA 清晰
- Tab 顺序合理
- 移动端输入体验良好

不要使用只有 placeholder、没有 label 的表单设计。

---

## 十、图标规范

一个项目优先使用一套主要图标库。

建议：

```text
普通 UI：16-20px
重点操作：20-24px
```

要求：

- 风格统一
- 描边粗细统一
- 对齐统一
- 不无意义堆砌

图标应辅助理解，不应成为纯装饰。

---

## 十一、导航规范

导航必须帮助用户快速理解：

- 当前在哪里
- 可以去哪里
- 当前页面属于哪个层级

要求：

- Active 状态明确
- 层级清晰
- 间距一致
- 减少无意义分割线
- 不让二级导航与一级导航竞争

---

## 十二、Dashboard / 中后台规范

Dashboard 优先考虑：

- 扫描效率
- 信息密度
- 操作效率
- 数据可读性
- 结构稳定
- 长期扩展能力

视觉方向可以参考 Stripe Dashboard。

### 推荐结构

```text
Sidebar
↓
Top Navigation
↓
Page Header
↓
Key Metrics
↓
Primary Content
↓
Table / Chart / Activity
```

指标卡应突出：

1. 核心数字
2. 变化趋势
3. 辅助说明

不要为了所谓“高级感”牺牲数据密度和操作效率。

---

## 十三、登录页规范

登录页最重要的目标：

> 帮助用户快速完成登录。

### Desktop 推荐结构

#### 方案 A

```text
┌──────────────────────────────┐
│          Login Form          │
└──────────────────────────────┘
```

#### 方案 B

```text
┌──────────────────────────────┐
│ Brand / Product │ Login Form │
└──────────────────────────────┘
```

### Mobile

优先采用单栏：

```text
Logo
↓
标题
↓
描述
↓
表单
↓
主 CTA
↓
次级登录方式
↓
协议 / 帮助
```

避免：

- 大量营销内容
- 复杂背景动画
- 多个抢夺注意力的 CTA
- 移动端保留无意义左侧宣传区

---

## 十四、Landing Page / 官网规范

Landing Page 优先考虑：

- Hero 层级
- 价值主张
- CTA
- 页面节奏
- 产品截图
- 社会证明
- 信息渐进披露

推荐结构：

```text
Navbar
↓
Hero
↓
Social Proof
↓
核心价值
↓
Feature
↓
产品截图
↓
Use Cases
↓
CTA
↓
Footer
```

可参考 Linear 风格，但避免生成模板化 AI Landing Page。

---

## 十五、响应式规范

至少检查：

```text
375px
768px
1024px
1280px
1440px
1920px
```

移动端不是简单缩小桌面端。

需要主动：

- 双栏转单栏
- 降低信息密度
- 减少装饰
- 保留核心操作
- 防止横向溢出
- 保证触控区域
- 优化表格、Tabs、Modal、Drawer

---

## 十六、动效规范

动效用于表达状态，不用于纯装饰。

推荐时长：

```text
150-250ms
```

适合：

- Hover
- Focus
- Menu
- Modal
- Dropdown
- Tab
- 状态切换

避免：

- 长时间动画
- 大幅缩放
- 无意义弹跳
- 持续运动
- 影响操作速度的动画

---

## 十七、可访问性

必须考虑：

- 文字对比度
- 键盘操作
- Focus 可见
- 语义化 HTML
- 正确 Label
- 合理点击区域
- Disabled 状态
- 错误提示
- Screen Reader 基础语义

不要为了视觉风格牺牲可访问性。

---

## 十八、工程实现规范

UI 任务开始前：

1. 检查项目结构。
2. 检查 UI 框架。
3. 检查现有 Design Token。
4. 检查公共组件。
5. 检查 CSS / Tailwind / CSS Modules / CSS-in-JS 使用方式。
6. 检查响应式体系。

实现时：

- 优先复用。
- 不重复造轮子。
- 重复值抽 Token。
- 避免大量 inline style。
- 避免无意义 absolute 定位。
- 避免通过 transform 强行对齐。
- 避免大量 `!important`。
- 避免为了一个页面创建平行的 Design System。

---

## 十九、Visual QA

完成 UI 修改后，必须执行视觉检查。

### 1. 层级

检查：

- 第一眼能否看到最重要信息？
- 主 CTA 是否明显？
- 页面标题是否明确？
- 次级信息是否合理弱化？

### 2. 间距

检查：

- 是否遵循统一间距尺度？
- 相关信息是否自然分组？
- 是否有局部拥挤？
- 是否有无意义大块空白？

### 3. 对齐

检查：

- 卡片边缘
- 表单控件
- 标题
- 按钮
- 图标
- 表格
- 内容容器

是否统一对齐。

### 4. 字体

检查：

- 是否存在过多字号？
- H1/H2/H3 是否明显？
- 正文是否易读？
- 次级文字是否过浅？

### 5. 组件

检查：

- Button 是否统一？
- Input 是否统一？
- Card 是否统一？
- Icon 是否统一？
- Radius 是否统一？

### 6. 状态

检查：

- Hover
- Focus
- Active
- Disabled
- Loading
- Error
- Empty

是否完整。

### 7. 响应式

重点检查：

```text
375
768
1024
1440
1920
```

是否存在：

- 横向溢出
- 文字截断
- Grid 破坏
- 卡片过宽
- 页面过空
- 操作按钮换行异常
- Modal 超出屏幕
- 表格无法操作

---

## 二十、Codex 视觉设计标准流程

处理视觉任务时，按照以下流程执行：

1. 阅读 `AGENTS.md`。
2. 阅读 `design.md`。
3. 检查当前页面和已有组件。
4. 分析主要视觉问题。
5. 确认视觉方向。
6. 完成第一版。
7. 启动项目。
8. 检查真实页面。
9. 执行 Visual QA。
10. 修正明显问题。
11. 再次检查。
12. 达到生产级视觉完成度后结束。

不要完成第一版代码后立即停止。

---

## 二十一、禁止的常见 AI UI 套路

默认不要使用：

- 紫色渐变满屏
- Glassmorphism 满屏
- 巨型圆角
- 所有按钮胶囊化
- 所有区块卡片化
- 随机 Glow
- 无意义 Floating Card
- 装饰 Blob
- 大量渐变文字
- 图标堆砌
- 所有内容居中
- 无意义超大 Hero 标题
- 大量装饰动画
- 与品牌无关的 3D 元素

除非项目本身明确需要。

---

## 二十二、最终完成标准

完成之前，必须问自己：

> 这个页面是否看起来像由专业产品设计团队有意识地设计，并已经达到可以进入生产环境的完成度？

如果答案是否定的，应继续优化。
