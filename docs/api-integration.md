# 商城后台接口对接清单

## 当前集成状态（2026-09-12，T12 第一批）

接口范围以 [任务清单](./swagger-parallel-tasks.md) 和 [冻结契约](./swagger-parallel-contracts.md) 的 83 操作基线为准。旧 13 接口清单已过时；不能据此认定后台没有短信、SKU、下单和支付接口。本工作区未包含忽略目录中的 Swagger 快照，尚未重新核对其哈希或进行真实后端联调。

| 范围 | 当前代码与装配状态 |
| --- | --- |
| 登录、注册、刷新、个人资料 | 保留既有接入及权限过滤 |
| T1 短信 | PhoneLogin 路由已接入短信页面及双语翻译 |
| T1 退出 | 个人资料操作区接入 LogoutButton 与双语翻译；由 Shell 保持挂载，防止本地退出移除受保护路由后丢失远端撤销结果。取消不发送请求，撤销失败不显示成功 |
| T2 扫码 | 复用现有 qr-login 插件和受保护路由，使用 JSON 凭证及 scan/confirm/cancel 新协议 |
| T3–T4 | 当前仍为旧商品与本地 productId 购物车；未交付新 SKU/购物车 port |
| T5–T9 | 优惠券、订单中心、售后未交付；结算和支付仍为旧协议，不代表接入新后台 |
| T10–T11 | 会员、消息、秒杀页面及公开 port 未交付，未注册空路由或虚构菜单 |

退出复用 SessionManager.signOutForRevocation 和 T1 服务，提交当前 refreshToken，关闭副作用自动重试。会话轮换、账号切换及不确定撤销行为由现有 T1/core 实现负责，不创建第二套会话机制。

当前仍缺少状态规则文件 `global-rules/AGENTS_condition_rules.md`。完整 T12 验收须在 T3–T11 交付后按冻结顺序继续；两品牌、双语、六种宽度真实渲染、真机及真实支付不能由 Jest 或 JS Bundle 替代。找回密码、资料编辑、地址簿、真实渠道拉起、秒杀结果查询及用户售后进度等契约缺口仍见任务清单。

以下为历史验收记录，日期与范围仅适用于当次改动，不能作为当前 T12 全量验收证据。

## 此前登录接入行为与验证结果

- 登录表单按后端要求使用邮箱和密码，保存用户 ID 与两类 Token；表单包含加载、错误提示、防重复提交及离页响应保护。
- Access Token 过期或鉴权请求返回 401 时，续期并保存轮换后的 Refresh Token；并发请求共享续期，失败不会无限刷新。网络或服务暂时故障保留会话，明确凭据失效才清理会话。
- 页面装配使用真实登录状态，取消原先固定的已登录状态和硬编码权限。未登录访问收银台进入账号密码登录，登录后购物车保留。扫码授权沿用已有 `requiresAuth` 限制，仅登录后可进入。
- 会话仍使用项目原有内存存储；本次未新增重启 App 后的持久登录能力。
- `npm run typecheck`、`npm run lint`、`npm test -- --watch=false` 全部通过：21 个测试套件、192 项测试。
- `npm run bundle:android` 与 `npm run bundle:ios` 均通过；覆盖 JS Bundle 构建，不代表原生安装包或真机验收。
- 本地接口探测确认：登录及刷新参数非法返回 `400 VALIDATION_FAILED`；不存在的测试账号返回 `401 INVALID_CREDENTIALS`；无效签名的测试 Token 返回 `401 REFRESH_TOKEN_INVALID`。未创建账号、订单或支付。
- 尚未提供真实账号，因此成功登录与成功轮换使用模拟 HTTP 响应做集成测试，尚未完成真实账号的成功链路联调；按要求未执行视觉验收。

## 注册功能接入（2026-09-08）

- 入口：登录方式页与账号密码登录页的“立即注册”；注册页面提供“已有账户？去登录”。注册是独立认证路由，不改变现有“开户”模块。
- 请求：匿名 `POST /api/v1/auth/register`，必填 `email`、`password`，支持可选 `displayName`；接口层同时保留文档中的可选 `phone`，当前注册页面不收集手机号。确认密码只用于客户端校验，不发送给服务端。
- 邮箱与昵称去除首尾空白；空昵称省略；密码保持原样。邮箱最多 320 字符，昵称最多 100 字符，密码 12–128 位且包含大小写字母、数字和符号。长度计算与后端保持一致，正确处理 emoji 代理对和变体选择符，输入框不会按 UTF-16 单元提前截断密码。
- **文档差异**：Swagger 仅标出密码长度；本地后台 `src/modules/auth/presentation/dto/register.dto.ts` 还使用 `IsStrongPassword`，要求 ASCII 大小写字母、数字以及指定符号。前端提示与强度校验已按后台实现补齐，中文、emoji 等不能替代所需类别。Swagger 只声明注册的 400 错误，但 `auth.service.ts` 中重复邮箱实际返回 `409 USER_ALREADY_EXISTS`。
- 成功：`201` 响应的 `data.user.id` 与 `data.tokens` 经统一验证后交给现有会话服务，进入首页；不额外调用登录接口，也不在客户端自行授予权限。
- 失败：区分重复邮箱、校验失败、429 限流和其他服务/网络故障；请求期间禁用表单并防重复提交。注册可能已在服务端完成，因此网络失败不自动重放 POST；页面离开后迟到的响应不写入会话或触发跳转。
- 回归检查：`npm run typecheck`、`npm run lint`、`npm test -- --watch=false` 通过，共 23 个测试套件、258 项测试，其中新增注册相关 66 项。`npm run bundle:android` 与 `npm run bundle:ios` 通过；首次 Android 构建受沙箱 `spawn EPERM` 限制，获准在沙箱外使用原命令后通过，未修改构建逻辑。
- 真实联调：在 Android 模拟器中使用本地开发服务创建了独立测试用户 `codex-register-20260908083120@example.invalid`；确认密码不一致时正确拦截，注册成功后进入首页，并显示需要登录的扫码授权入口。同邮箱再次注册显示已注册提示，返回账号密码登录入口可用。未触发订单、支付或外部通知。
- 视觉与交互验收：Android 模拟器检查了 375、768、1024、1280、1440、1920 逻辑宽度，覆盖中英文、空表单、禁用、焦点、校验错误、重复邮箱及成功状态。宽屏表单保持最大 480 逻辑像素；小屏内容可滚动，无横向溢出。修正 Android 软键盘遮挡：按表单实际顶部位置避让，375 宽度下键盘展开时仍可滚动到昵称、提交按钮与返回登录入口，并实际验证返回登录可点击。验收后恢复原始 1080×2400 / 420 dpi，停在中文注册页；临时测试凭据文件已删除，截图保存在忽略目录 `artifacts/registration-qa/`。
- 会话继续沿用现有内存存储，重启 App 后需重新登录。iOS 此次仅验证 JS Bundle 构建，尚未执行 Xcode 或 iOS 真机验收。

## 个人资料功能接入（2026-09-08）

- Aurora 与 Cedar 登录后均可从首页快捷入口或“更多”进入个人资料；路由、首页与菜单入口受登录状态保护，保留现有登录和扫码入口。
- 页面通过带 Bearer Token 的 `GET /api/v1/users/me` 获取资料，显示昵称、邮箱、手机号、账户状态、用户 ID、注册时间和更新时间。空昵称使用账户占位名称，未填写字段显示“未设置”。当前后端没有资料修改接口，因此提供查看、下拉刷新与手动刷新。
- 资料不进入共享缓存，按当前用户 ID 隔离；切换账号立即隐藏旧资料，卸载或切换账号后的迟到响应不再更新页面。请求期间防止重复刷新；首次失败提供重试，刷新失败保留上次资料并明确提示。资料响应不会修改会话权限或 Token。
- 复用品牌颜色、间距与圆角，支持中英文与 375、768、1024、1280、1440、1920 逻辑宽度。状态使用语义色圆点和高对比度文字；日期使用设备时区，并通过 Intl 的 `lookup` 匹配跟随应用语言，解决当前 Android Hermes 将 `zh-CN` 默认回退成系统英文的问题。
- 验证：`npm run typecheck`、`npm run lint`、`npm test -- --watch=false` 全部通过，共 26 个测试套件、311 项测试；品牌配置校验以及 Android / iOS JS Bundle 构建通过。Metro 构建最初受沙箱 `spawn EPERM` 限制，使用原命令在沙箱外验证通过，未修改构建逻辑。
- Android 模拟器使用本地开发测试账户完成真实登录与资料读取，检查上述宽度、中英文、长邮箱、空手机号、返回首页和“更多”入口。临时断开该模拟器的 3000 端口映射，验证刷新失败保留资料、首次加载失败和恢复连接后重试成功；映射已恢复。验收后恢复原始 1080×2400 / 420 dpi，停在中文个人资料页，临时测试凭据已删除。截图保存在忽略目录 `artifacts/profile-qa/`。
- 未增加生产依赖或登录持久化；本次未修改后端、接入资料编辑或退出登录，也未执行 iOS 原生验收。


### 本批验证结果

- `npm run typecheck`、`npm run lint` 通过。
- `npm test -- --watch=false`：37 套、476 项通过。
- `npm run bundle:android`、`npm run bundle:ios`：沙箱中 Metro 子进程 `spawn EPERM`，获准后原命令在沙箱外通过；未修改构建配置。
- 退出失败回归发现并修复公共 HttpClient 对空/非 JSON/null 错误响应解引用异常，保留原 HTTP 状态与 HTTP_ERROR；新增三项覆盖，不改变重试策略。
- 本工作区起初没有 node_modules，通过现有 package-lock 安装开发依赖，未新增生产依赖或修改 lock。
- `adb devices` 没有连接设备；本批未执行两品牌/双语/六宽度真实渲染、原生包或真实后端联调。上述检查不构成 T12 最终验收。
